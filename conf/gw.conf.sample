gwaves = 
{
    // General settings.
    general = 
    {
        frequency = 20;
        timestep = 4.0;
    };

    // Display configuration.
    display = 
    {
        // Screen dimensions.
        screen = 
        {
            width = 500;
            height = 500;
        };

        // Background color. 
        // Color format, in hex; 
        //        0xRRGGBB
        bgcolor = 0x000000;
    };

    // Body configuration.
    body =
    {
        // default mass.
        mass = 500.0;
        display = 1;
        //      0xRRGGBB
        color = 0xffffff;
    };

    // Wave configuration.
    wave = 
    {
        speed = 5.0;
        lifetime = 100.0;
        display = 1;
        //      0xRRGGBB
        color = 0x0080ff;
    };

    // Universe configuration.
    universe =
    {
        radius = 1000.0;
        gravity_con = 1.0;
        num_bodies = 10;
    };
    
};

